# name: test/sql/otlp.test
# description: test otlp extension
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT otlp('Sam');
----
Catalog Error: Scalar Function with name otlp does not exist!

# Require statement will ensure this test is run with this extension loaded
require otlp

# Confirm the extension works
query I
SELECT otlp('Sam');
----
OTLP Sam 🐥

# Verify helper table functions are registered
query I
SELECT COUNT(*) > 0 FROM duckdb_functions() WHERE function_name LIKE 'read_otlp_%';
----
true
